services:
  api:
    image: ghcr.io/tkob-team/tkob_qrordersystem:latest
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - ./.env  # Load environment variables from .env file
    depends_on:
      - redis
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  redis_data:
