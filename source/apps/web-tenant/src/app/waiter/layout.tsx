'use client';

/**
 * Waiter Layout Wrapper
 * ServiceBoardPage is a full-screen component that manages its own layout
 */
export default function WaiterLayoutWrapper({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
