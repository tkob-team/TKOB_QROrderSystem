/**
 * Landing Feature - Type Definitions
 * Minimal types for this presentation-only feature
 */

// Reserved for future landing-specific types
// Currently no custom types needed (uses shared Session type from context)

// Export empty object to make this a valid module
export {}
