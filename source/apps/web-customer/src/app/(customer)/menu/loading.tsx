/**
 * Menu Page Loading State
 * 
 * Displays skeleton loader while menu data is being fetched
 */

import { MenuSkeleton } from '@/shared/components/skeletons'

export default function MenuLoading() {
  return <MenuSkeleton />
}
