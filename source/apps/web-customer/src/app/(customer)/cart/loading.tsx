/**
 * Cart Page Loading State
 * 
 * Displays skeleton loader while cart data is being fetched
 */

import { CartSkeleton } from '@/shared/components/skeletons'

export default function CartLoading() {
  return <CartSkeleton />
}
