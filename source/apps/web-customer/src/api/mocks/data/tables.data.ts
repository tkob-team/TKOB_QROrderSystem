// Mock table data

import { Table } from '@/types';

export const mockTable: Table = {
  id: 'table-12',
  number: '12',
  guestCount: 2,
  sessionToken: 'mock-token-123',
};

export const mockTables: Table[] = [
  mockTable,
  // Can add more tables for testing
];
